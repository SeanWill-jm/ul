# Package Installation Notes

Installable addon folder: `purchase_warehouse_delivery_address/`

Target: **Odoo.sh 19.0**

Static validation performed during packaging:

- Python syntax check.
- XML well-formedness check.
- Manifest parse check.
- ZIP integrity check.

This artifact-generation environment does not contain a running Odoo 19 database, so runtime installation must be validated first in Odoo.sh Development/Staging.
